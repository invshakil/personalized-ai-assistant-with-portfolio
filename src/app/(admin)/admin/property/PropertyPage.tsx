import StubPage from "@/components/admin/StubPage";

export default function PropertyPage() {
  return (
    <StubPage
      title="Property Management"
      description="Manage your rental units, track tenants, and monitor monthly payments. Mark rent as paid, partial, or overdue — all in one place."
      icon="🏠"
    />
  );
}
