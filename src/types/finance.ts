// Financial tracker — shared types. Re-exported through the @/types barrel.
// Most finance shapes are inferred from the service layer; only cross-surface
// enums live here.

export type IncomeCategory = "SALARY" | "FREELANCE" | "RENTAL" | "OTHER";
